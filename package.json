{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "description": "A Next.js + Gun.js app",
  "scripts": {
    "preci": "rm -rf node_modules",
    "ci": "yarn install --frozen-lockfile",
    "build": "npx lerna run build",
    "lint": "npx lerna run lint",
    "lint:fix": "npx lerna exec -- yarn lint:fix",
    "no-lock": "npx lerna exec -- yarn no-lock"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BryanPan342/pewpew.git"
  },
  "author": "Bryan Pan",
  "license": "MIT",
  "homepage": "https://github.com/BryanPan342/pewpew/#readme",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
